# 虚拟环境配置

本文件记录当前项目使用的 Python 虚拟环境，方便后续继续开发或让 Codex 识别项目环境。

## 当前环境

- 环境管理器：conda
- 环境名称：mineru
- Python 版本：3.12.0
- 环境路径：`C:\Users\LZ-DSJ-01\miniconda3\envs\mineru`
- 创建命令：`conda create --name mineru python=3.12.0`

## 项目版本要求

项目 `pyproject.toml` 声明：

```toml
requires-python = ">=3.10,<3.14"
```

README 推荐 Python 版本范围为 3.10-3.13。当前 `mineru` 环境的 Python 3.12.0 符合项目要求。

## 使用方式

激活环境：

```powershell
conda activate mineru
```

验证环境：

```powershell
python --version
```

或不激活环境直接验证：

```powershell
conda run -n mineru python --version
```

## 依赖安装状态

已在 `mineru` 环境中安装当前项目源码包及 `[all]` 依赖。

- 安装方式：editable 源码安装
- 安装命令：`conda run -n mineru python -m uv pip install -e ".[all]" -i https://mirrors.aliyun.com/pypi/simple`
- 已验证包版本：`mineru==3.1.15`
- 已验证命令：`mineru --help`
conda run -n mineru pip install uv -i https://mirrors.aliyun.com/pypi/simple
conda run -n mineru python -m uv pip install -e ".[all]" -i https://mirrors.aliyun.com/pypi/simple
conda run -n mineru python -m uv pip install -e ".[all]" -i https://mirrors.aliyun.com/pypi/simple
如需重新安装或同步依赖，可在项目根目录执行：

```powershell
conda activate mineru
uv pip install -e ".[all]" -i https://mirrors.aliyun.com/pypi/simple
```

## 注意

当前项目后续应优先使用 `mineru` 这个 conda 环境。若项目目录下存在 `.conda\mineru-py312`，它来自此前未完成的项目内环境创建尝试，不作为当前配置环境使用。

## 模型下载状态

已使用 `mineru` 环境从 ModelScope 下载 MinerU 模型，并自动写入用户目录配置文件：

- 配置文件：`C:\Users\LZ-DSJ-01\mineru.json`
- 下载命令：`conda run -n mineru mineru-models-download --source modelscope --model_type pipeline`
- 下载命令：`conda run -n mineru mineru-models-download --source modelscope --model_type vlm`

本地模型路径：

- pipeline：`C:\Users\LZ-DSJ-01\.cache\modelscope\hub\models\OpenDataLab\PDF-Extract-Kit-1___0`
- vlm：`C:\Users\LZ-DSJ-01\.cache\modelscope\hub\models\OpenDataLab\MinerU2___5-Pro-2604-1___2B`

当前模型缓存大小：

- pipeline：约 2.32 GB
- vlm：约 2.17 GB

`mineru.json` 中的 `models-dir` 已配置为上述路径。

## CUDA 配置状态

当前 `mineru` 环境已将 CPU 版 PyTorch 替换为 CUDA 12.9 版 PyTorch，并安装了 conda 内 CUDA Toolkit 12.9。

- 显卡：NVIDIA GeForce RTX 5070 Laptop GPU
- NVIDIA 驱动：576.65
- `nvidia-smi` 显示 CUDA：12.9
- conda 环境 CUDA Toolkit：12.9
- conda 环境 CUDA 路径：`C:\Users\LZ-DSJ-01\miniconda3\envs\mineru\Library`
- 系统 CUDA Toolkit：13.2
- 系统 CUDA 路径：`C:\Program Files\NVIDIA GPU Computing Toolkit\CUDA\v13.2`
- PyTorch：`torch==2.8.0+cu129`
- TorchVision：`torchvision==0.23.0+cu129`
- TorchAudio：`torchaudio==2.8.0+cu129`
- 安装命令：`conda run -n mineru python -m pip install --force-reinstall torch==2.8.0 torchvision==0.23.0 torchaudio==2.8.0 --index-url https://download.pytorch.org/whl/cu129`
- CUDA Toolkit 安装命令：`conda install -n mineru -c nvidia cuda-toolkit=12.9 -y`

验证命令：

```powershell
conda run -n mineru python -c "import torch; print(torch.__version__); print(torch.version.cuda); print(torch.cuda.is_available()); print(torch.cuda.get_device_name(0))"
```

验证结果：

```text
torch 2.8.0+cu129
torch.version.cuda 12.9
cuda_available True
device_name NVIDIA GeForce RTX 5070 Laptop GPU
```

LMDeploy/TurboMind 在 Windows 上要求 `CUDA_PATH`。推荐启动前显式使用 conda 环境内 CUDA 12.9，避免系统 CUDA 13.2 和 PyTorch CUDA 12.9 混用：

```powershell
$env:CUDA_PATH="C:\Users\LZ-DSJ-01\miniconda3\envs\mineru\Library"
$env:Path="$env:CUDA_PATH\bin;$env:Path"
$env:TRITON_CACHE_DIR="C:\Users\LZ-DSJ-01\Desktop\work\git_work\mineru\MinerU\.triton-cache"
$env:MINERU_MODEL_SOURCE="local"
conda run -n mineru mineru-gradio --server-name 127.0.0.1 --server-port 7860
```

已验证：

```text
lmdeploy.turbomind import ok
```

RTX 5070 上 `lmdeploy` 的 `turbomind` 后端会报 `no kernel image is available for execution on the device`，因此使用 `lmdeploy` 时推荐改为 PyTorch 后端，并配置 Triton 缓存目录：

```powershell
$env:CUDA_PATH="C:\Users\LZ-DSJ-01\miniconda3\envs\mineru\Library"
$env:Path="$env:CUDA_PATH\bin;$env:Path"
$env:TRITON_CACHE_DIR="C:\Users\LZ-DSJ-01\Desktop\work\git_work\mineru\MinerU\.triton-cache"
$env:MINERU_MODEL_SOURCE="local"
$env:MINERU_LMDEPLOY_BACKEND="pytorch"
$env:MINERU_LMDEPLOY_DEVICE="cuda"
conda run -n mineru mineru-api --host 127.0.0.1 --port 8000
```

Gradio 单独连接 API：

```powershell
$env:CUDA_PATH="C:\Users\LZ-DSJ-01\miniconda3\envs\mineru\Library"
$env:Path="$env:CUDA_PATH\bin;$env:Path"
$env:TRITON_CACHE_DIR="C:\Users\LZ-DSJ-01\Desktop\work\git_work\mineru\MinerU\.triton-cache"
$env:MINERU_MODEL_SOURCE="local"
conda run -n mineru mineru-gradio --server-name 127.0.0.1 --server-port 7860 --api-url http://127.0.0.1:8000
```

已安装并验证：

```text
triton-windows==3.4.0.post21
python -m lmdeploy.pytorch.check_env.triton_custom_add -> Done.
torch.compile CUDA smoke test -> ok
```

额外修复：YAPF 在 Windows 上首次生成语法缓存时可能卡住，已手动生成以下缓存文件：

- `C:\Users\LZ-DSJ-01\AppData\Local\Google\YAPF\Cache\0.43.0\Grammar-py3.12.0.final.0.pickle`
- `C:\Users\LZ-DSJ-01\AppData\Local\Google\YAPF\Cache\0.43.0\PatternGrammar-py3.12.0.final.0.pickle`



mineru -p "C:\path\to\input.pdf" -o ".\output" -b pipeline -m auto -l ch

mineru -p "C:\path\to\input.pdf" -o ".\output" -b vlm-auto-engine

conda activate mineru


前端启动
$env:CUDA_PATH="C:\Users\LZ-DSJ-01\miniconda3\envs\mineru\Library"
$env:Path="$env:CUDA_PATH\bin;$env:Path"
$env:MINERU_VLM_ENGINE="transformers"
$env:MINERU_MODEL_SOURCE="local"
conda run -n mineru mineru-gradio --server-name 127.0.0.1 --server-port 7860 --api-url http://127.0.0.1:8000


http://127.0.0.1:7860/


服务端启动
$env:CUDA_PATH="C:\Users\LZ-DSJ-01\miniconda3\envs\mineru\Library"
$env:Path="$env:CUDA_PATH\bin;$env:Path"
$env:MINERU_MODEL_SOURCE="local"
$env:MINERU_VLM_ENGINE="transformers"
conda run -n mineru mineru-api --host 127.0.0.1 --port 8000


## Linux 启动命令

conda activate mineru


conda run -n mineru mineru-gradio --server-name 0.0.0.0 --server-port 8100 --api-url http://127.0.0.1:8000

nohup conda run --no-capture-output -n mineru mineru-gradio --server-name 0.0.0.0 --server-port 8100 --api-url http://127.0.0.1:8000 > mineru.log 2>&1 &

export MINERU_VLM_ENGINE="vllm-async"
export MINERU_MODEL_SOURCE="local"
conda run -n mineru mineru-api --host 127.0.0.1 --port 8000
nohup conda run --no-capture-output -n mineru mineru-api --host 127.0.0.1 --port 8000 > mineru-api.log 2>&1 &
192.168.16.66:8100



transformers	transformers	HuggingFace Transformers，最通用，速度通常较慢
vllm	vllm-engine	vLLM 同步引擎，本地加速
vllm-async	vllm-async-engine	vLLM 异步引擎，跑 mineru-api 更建议这个
lmdeploy	lmdeploy-engine	LMDeploy 引擎，Linux/Windows 都有相关支持
mlx	mlx-engine	Apple Silicon macOS 专用，Linux 不用这个